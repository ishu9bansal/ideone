{
    "configurations": [
        {
            "name": "C/C++: g++ build and debug active file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/../.local/${relativeFileDirname}/${fileBasenameNoExtension}",
            "args": ["../../${relativeFileDirname}"],
            "stopAtEntry": false,
            "cwd": "${fileDirname}/../.local/${relativeFileDirname}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "C/C++: g++ build active file"
        }
    ],
    "version": "2.0.0"
}